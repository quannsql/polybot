app = "polybot-63-npg"
primary_region = "arn"

[build]
  dockerfile = "Dockerfile.preflight"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

# The preflight is deliberately one-shot. A clean exit must leave the Machine stopped.
[[restart]]
  policy = "never"
